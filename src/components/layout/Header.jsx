import styled from "styled-components"

const StyledAppHeader = styled.header `
`

const Header = () => {
  return (
    <StyledAppHeader>
      Header
    </StyledAppHeader>
  )
}

export default Header